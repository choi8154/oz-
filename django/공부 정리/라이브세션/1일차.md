# 강의 내용

### 1) url 단축 서비스
- bitly : 비틀리 서비스를 사용해서 긴 url주소를 짧게 만들 수 있음.

### 2) 등록 되어있는 기본 앱
- 기본으로 주어지는 앱 모듈로써 이걸 활용해서 사용 가능
- 이 모듈들은 기본적으로 세팅이 되어있음.
```python
INSTALLED_APPS = [
    'django.contrib.admin',
    'django.contrib.auth',
    'django.contrib.contenttypes',
    'django.contrib.sessions',
    'django.contrib.messages',
    'django.contrib.staticfiles',
    'app'
]
```

### 3) Meta
- 라는 이름으로 클레스를 선언하면
- 장고는 Meta에 정의된 속성에 따라서 테이블의 속성을 정의함.


대답할 것
python manage.py createsuperuser
python manage.py makemigrations
python manage.py migrateㅁ